spring.application.name=frogger

#  Deja que Railway pase los datos de conexión desde las variables de entorno
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

#  Hibernate validará la estructura (no borra ni crea la BBDD)
spring.jpa.hibernate.ddl-auto=validate

#  Tu clave sigue igual
SECRET_KEY=lMCvj7Sirkk41OpuXDBKoSA1YeQ4aTeHmP4gzoyoaLk=

#  Evita que ejecute automáticamente los scripts
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never

#  Dialecto moderno de MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# (Opcional, si quieres ver las consultas en consola)
# spring.jpa.show-sql=true

# Puerto por defecto
server.port=8080
